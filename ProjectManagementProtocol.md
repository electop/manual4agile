# 📘 파일 기반 데이터 분석 및 집계 프로토콜 (통합 v3.1)

## 🔄 변경 이력
| 버전 | 일자 | 주요 변경 내용 | 작성자 |
|------|------|----------------|--------|
| v3.0 | 2024-09-01 | 최초 통합 버전 | SWITCH Secretariat |
| **v3.1** | **2025-10-14** | **혼합 소속 과제 누락 방지 로직 추가 (과제번호 우선 탐색)** | **유코치 제안 반영** |

---

## 0️⃣ 목적
본 문서는 `ProjectMember+.md`와 `ProjectStatusNew+.md`를 기반으로  
과제 참여자·성과 정보를 분석할 때, **정확성(Accuracy)** 과 **재현성(Reproducibility)** 을 보장하기 위한 표준이다.

---

## 1️⃣ 파일 기반 응답 정확도 유지 가이드

| 항목 | 원칙 |
|------|------|
| **1. 데이터 원천 제한** | 응답은 반드시 업로드된 파일 내 실제 데이터만을 기반으로 해야 한다. (추측·환각 생성 금지) |
| **2. 키값 기반 탐색** | **v3.1 개선:** 모든 탐색은 `과제 번호`를 1차 키로 수행하고, 이후 `사업부`·`이름` 등 2차 필터를 적용한다. |
| **3. 혼합 소속 과제 처리** | **v3.1 신설:** 하나의 과제 내 여러 사업부가 공존할 경우, `사업부별 필터링`은 반드시 `과제번호 전체행 조회 이후` 수행한다. |
| **4. 미존재 데이터 처리** | 값이 없을 경우 `"모름"` 또는 `"데이터에 존재하지 않습니다."`로 명시한다. |
| **5. Separator 규칙** | 구성원 필드의 구분자는 쉼표(,)만 허용하며, 공백은 구분자로 사용하지 않는다. |
| **6. 다중 구성원 표기** | `<br>` 태그로 구분하며, 각 항목은 `이름(Knox ID, 사업부, 시작일, 완료일)` 형식을 따른다. |
| **7. 환각 방지** | 파일에 존재하지 않는 인물, 사업부, 과제명, 날짜, 수치는 절대 생성하지 않는다. |
| **8. 불완전 행 처리** | 파이프(`|`) 개수 불일치 등 비정상 행은 분석에서 제외하고 로그로 기록한다. |
| **9. 답변 불가능 시 대응** | 데이터 불완전 시 “파일 내 데이터가 불완전하여 답변 불가”로 명시한다. |

---

## 2️⃣ 데이터 병합 및 구조 규칙

| 항목 | 설명 |
|------|------|
| **1. 병합 키** | `과제 번호`를 기준으로 `ProjectMember+.md`와 `ProjectStatusNew+.md` 병합 |
| **2. 우선순위** | `사업부_m`(Member 파일)이 우선이며 누락 시 `사업부_s`(Status 파일) 사용 |
| **3. 날짜 정규화** | `시작일`의 연도를 추출하여 `연도` 필드 생성 |
| **4. 중복 참여 정의** | 동일 Knox ID가 2개 이상 과제에 등장하면 **다수참여자**로 정의 |
| **5. 정렬 기준** | `연도 → 사업부 → 이름` 순 |

---

## 3️⃣ 연도별·사업부별 참여자수 집계 규칙 (v3.1)

| 항목 | 설명 |
|------|------|
| **집계 단위** | 연도별 × 사업부별 |
| **연도 기준** | 과제 시작일 기준 |
| **표기 형식** | `총원(다수참여)` 예: `68(5)` |
| **합계 계산** | 연도별·사업부별 동일 형식으로 표기 |
| **사업부 기준** | 멤버 파일(`사업부_m`) 우선, 결측 시 상태 파일(`사업부_s`) 사용 |
| **혼합 소속 처리** | **v3.1 신설:** 한 과제 내 다수 사업부 존재 시, 중복 없이 모든 인원을 각 사업부에 분배 집계 |

---

## 4️⃣ 다수참여자 집계 로직 (v3.1 동일)

1. 전체 기간(2021–2025) 동안 Knox ID가 2회 이상 등장하면 다수참여자로 설정  
2. 실제 2회 이상 참여한 해부터 `다수참여자수`에 포함  
3. 첫 참여 해는 제외  
4. 세 번째 이상 참여해도 계속 포함  
5. 표기 예시: `42(3)` → 총 42명 중 3명이 두 번째 이상 참여  

---

## 5️⃣ 예외 및 경고 처리

| 상황 | 출력 규칙 |
|------|-----------|
| 사업부 없음 | `"⚠️ [과제명]의 사업부는 데이터에 존재하지 않습니다."` |
| 비정규 행 | `"⚠️ 형식 오류로 제외됨"` |
| 파일 간 불일치 | `"⚠️ 파일 간 매칭되지 않음 (과제 번호: NNN)"` |
| **혼합 소속 누락 경고 (v3.1)** | `"⚠️ 혼합 소속 과제 – 일부 사업부 누락 가능성 탐지됨 (과제 번호: NNN)"` |

---

## 6️⃣ 출력 예시

| 연도 | MX | SR | VD | DPC | BDC | DA | 의료기기 | 네트워크 | 전장 | 생기연 | 신사업팀 | GCS | 합계 |
|------|----|----|----|-----|-----|----|--------|--------|-----|-------|-------|-----|-----|
| **2022** | 56(2) | 31(3) | 16(1) | 7(1) | 11(1) | 3(0) | 7(0) | 15(1) | 1(0) | 4(0) | 0(0) | 0(0) | **151(8)** |

---

## 7️⃣ 출력 형식 요약
- 기본 표기: `총원(다수참여)`  
- 사업부 누락: ⚠️ 경고 문구 표시  
- 혼합 소속: `<br>`로 구분  
- 파일 불일치: “⚠️ 데이터 불일치” 명시 후 제외  

---

## 8️⃣ 자동 파일 로드 및 탐색 순서 (v3.1 수정)
1. 요청 발생 시, 모든 업로드 파일 본문 자동 로드  
2. **1차 키: `과제 번호`로 전체 행 검색**  
3. **2차 필터: `사업부`·`이름` 등 조건 적용**  
4. **혼합 소속 탐지 시** 자동으로 `사업부별 세분화 병합` 수행  
5. 오류 시 `"⚠️ 혼합 소속 탐색 실패"` 로그 출력  

---

📌 **v3.1 주요 개선 요약**
- ✅ **“과제번호 우선 탐색”** 원칙 도입  
- ✅ **혼합 소속 과제 자동 감지 및 분배 집계**  
- ✅ **누락 경고 메시지 표준화**

---

_Revision Author: 유코치 (Yoo Coach)  
for SWITCH Secretariat, 2025-10-14_
