# 📘 파일 기반 데이터 분석 및 집계 프로토콜 (통합 v3.0)

## 0️⃣ 목적
이 문서는 `ProjectMemberNew.md`와 `ProjectStatusNew+.md` 파일을 기반으로  
과제 참여자 정보를 분석할 때, **정확도(Accuracy)** 와 **재현성(Reproducibility)** 을 보장하기 위한 통합 기준이다.  
모든 응답은 이 가이드의 원칙을 따르며, 데이터 외삽이나 추정값을 생성하지 않는다.

---

## 1️⃣ 파일 기반 응답 정확도 유지 가이드

| 항목 | 원칙 |
|------|------|
| **1. 데이터 원천 제한** | 응답은 반드시 업로드된 파일 내의 실제 데이터만을 기반으로 해야 한다. (추측·환각 생성 금지) |
| **2. 키값 기반 탐색** | 모든 탐색은 `과제번호`를 기본 키로 수행한다. |
| **3. 미존재 데이터 처리** | 파일 내에 값이 존재하지 않는 경우, `"모름"` 또는 `"데이터에 존재하지 않습니다."`로 명시한다. |
| **4. Separator 규칙** | 구성원 정보 필드의 구분자는 반드시 쉼표(,). 공백(space)은 구분자로 사용하지 않는다. |
| **5. 다중 데이터 처리** | 한 셀 내 다수 구성원은 `<br>` 태그로 구분되어 있으며, 각 항목은 **이름(Knox ID, 사업부, 시작일, 완료일)** 형식을 따른다. |
| **6. 환각 방지** | 파일에 존재하지 않는 인물, 사업부, 과제명, 날짜, 수치는 절대 생성하지 않는다. |
| **7. 불완전 행 처리** | 파이프(`|`) 개수 불일치, 쉼표 누락 등 비정상 행은 분석에서 제외하고 로그로 기록한다. |
| **8. 답변 불가능 시 대응** | 데이터가 없거나 손상된 경우, “파일 내 데이터가 불완전하여 답변 불가”로 응답한다. |

---

## 2️⃣ 데이터 병합 및 구조 규칙

| 항목 | 설명 |
|------|------|
| **1. 병합 키** | `과제번호`를 기준으로 `ProjectMemberNew.md`와 `ProjectStatusNew+.md`를 병합한다. |
| **2. 우선순위** | `사업부_m`(Member 파일)이 우선이며, 누락 시 `사업부_s`(Status 파일)을 사용한다. |
| **3. 날짜 필드 정규화** | `시작일`의 첫 4자리를 기준으로 연도를 추출하여 `연도` 필드를 생성한다. |
| **4. 중복 참여 정의** | Knox ID가 서로 다른 과제번호에 2회 이상 등장하면 **다수참여자**로 정의한다. |
| **5. 데이터 정렬 기준** | 기본 정렬은 `연도 → 사업부 → 이름` 순으로 한다. |

---

## 3️⃣ 연도별·사업부별 참여자수 집계 규칙 (v3.0)

| 항목 | 설명 |
|------|------|
| **집계 단위** | 연도별 × 사업부별 |
| **연도 기준** | `시작일`의 연도를 기준으로 한다. |
| **표기 형식** | `총원(다수참여)` 예: `68(5)` → 총 68명 중 5명이 다수참여자 |
| **합계 계산** | 연도별, 사업부별 합계를 동일한 형식으로 표기 (`총원(다수참여)`) |
| **사업부 기준** | 멤버 파일(`사업부_m`)을 기본으로 사용하며, 결측 시 상태 파일(`사업부_s`) 사용 |

---

## 4️⃣ 다수참여자 집계 로직 (v3.0 세부 규칙)

| 단계 | 처리 내용 |
|------|------------|
| **① 다수참여자 식별** | 전체 기간(2021–2025) 동안 Knox ID가 2회 이상 등장한 인원을 다수참여자로 설정 |
| **② 연도별 판단 기준** | 다수참여자가 실제로 **2회 이상 참여한 해부터** 해당 연도의 `다수참여자수`에 포함 |
| **③ 첫 참여 해 처리** | 첫 참여 연도는 `다수참여자수`에 포함하지 않음 |
| **④ 누적 참여 해 처리** | 세 번째 이상 참여한 해에도 계속 포함 |
| **⑤ 표기 예시** | `42(3)` → 총 42명 중 3명이 해당 연도 기준으로 두 번째 이상 참여 |
| **⑥ 출력 순서** | 연도별 오름차순, 사업부별 오름차순 |

---

## 5️⃣ 예외 및 경고 처리

| 상황 | 출력 규칙 |
|------|-----------|
| **사업부 정보 없음** | `"⚠️ [과제명]의 사업부는 데이터에 존재하지 않습니다."` |
| **비정규 포맷 행** | 분석 대상에서 제외하고 `"⚠️ 형식 오류로 제외됨"`으로 로그 기록 |
| **데이터 불일치 시** | “⚠️ 파일 간 매칭되지 않음 (과제번호: NNN)” 로 표시 |

---

## 6️⃣ 출력 예시

| 연도 | MX | SR | VD | DPC | 합계 |
|------|------|------|------|------|
| **2021** | 39(0) | 1(0) | 11(0) |  | **81(0)** |
| **2022** | 68(2) | 31(4) | 16(1) |  | **162(10)** |
| **2023** | 56(3) | 71(5) | 9(1) |  | **186(13)** |
| **2024** | 2(0) |  |  | 101(1) | **214(10)** |
| **2025** | 9(1) |  |  | 72(2) | **216(14)** |
| **총합** | 174(7) | 103(9) | 36(3) | 173(3) | **856(54)** |

---

## 7️⃣ 출력 형식 요약

- **기본 표기:** `총원(다수참여)`  
- **사업부 누락:** ⚠️ 경고 문구 표시  
- **개행 규칙:** 여러 과제는 `<br>` 태그로 줄바꿈  
- **파일 불일치:** “⚠️ 데이터 불일치” 명시 후 제외 처리  

---

📌 **이 프로토콜(v3.0)** 은  
이후 `ProjectMemberNew.md` + `ProjectStatusNew+.md` 기반  
모든 집계·시각화·리포팅의 표준으로 사용된다.
