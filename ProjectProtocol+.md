# 📘 파일 기반 데이터 분석 및 집계 프로토콜  
(통합 **v3.5 – Precision Mode Edition**, Fail-Safe + Temperature Control 완전판)

---

## 0️⃣ 목적
- **환각(파일 비기반 생성) 절대 금지.**
- `ProjectMember+.md`, `ProjectInfo+.md`를 **유일한 데이터 원천**으로 사용.
- 모든 출력은 **증거 로그(Evidence)** 와 함께 제공.
- *(v3.4 추가)* **Fail-Safe 자동 복구 시스템** 적용: 파일 접근 실패·환각 감지 시 자동 재시도 또는 Manual Copy Mode 전환.
- *(v3.5 추가)* **Temperature 제어 시스템** 도입으로 모델 창의성 억제 및 데이터 일관성 강화.

---

## 1️⃣ 절대 규칙 (Hard Stops)

| 번호 | 규칙 | 오류코드 | v3.5 변경점 |
|------|------|-----------|-------------|
| 1 | **비데이터 생성 금지** – 파일에 없는 과제명·사업부·인물·수치 생성 시 즉시 중단 | `E100:NO_DATA` | *(v3.4)* 자동 재탐색 후 Manual Copy Mode 전환 |
| 2 | **미병합 질의 금지** – ‘과제 번호’ 병합 없이 “사업부”·“성과” 질의 금지 | `E110:REQUIRES_JOIN` | 동일 |
| 3 | **불확실 응답 금지** – “아마”, “추정됨” 사용 금지 | `E120:UNCERTAIN` | 동일 |
| 4 | **증거 누락 금지** – 결과 하단 Evidence 미포함 시 출력 금지 | `E130:MISSING_EVIDENCE` | *(v3.4)* 자동 재샘플 시도 |
| 5 | **Temperature 초과 금지** – `temperature > 0.2` 사용 시 오류 | `E121:TEMP_OVERRANGE` | *(v3.5 신규 추가)* |

---

## 2️⃣ 파이프라인 (Gate Sequence)

| 단계 | 설명 | 실패 시 코드 | v3.5 변경점 |
|------|------|---------------|-------------|
| **G1. 로드·스키마 검증** | Markdown 표 헤더(`과제 번호`)·파이프(`|`) 정합성 점검 | `E200:LOAD_FAIL` | *(v3.4)* 실패 시 3회 자동 재시도 |
| **G1-F. Manual Mode** | 사용자가 직접 표를 붙여넣은 경우 | — | *(v3.4 추가)* “수동 복사 입력”으로 간주, G1~G2 자동 통과 |
| **G2. 키 일치율 검증** | ‘과제 번호’ 교집합·여집합 계산 | `E210:NO_KEYS_IN_COMMON` | *(v3.4)* 여집합 제외 후 교집합만 병합 |
| **G3. 병합(JOIN)** | 교집합 키로 병합, 충돌 시 해당 키 제외 | `E220:CONFLICT:key=NN` | 동일 |
| **G4. 필터 적용** | 조건 예: “사업부=DPC” 등 | `E240:FILTER_NO_MATCH` | 동일 |
| **G5. 집계/가공** | 연도=시작일 연도, 다수참여=2회 이상 포함 | — | 동일 |
| **G6. 증거 로그 출력** | 파일명·행수·샘플행 출력 | `E130:MISSING_EVIDENCE` | *(v3.4)* 표 하단 + 원문 코드블록 병렬 출력 |
| **G7. 최종 출력** | 데이터 원천·Evidence 각주 포함 | — | 동일 |
| **G8. Fallback(신규)** | *(v3.4 추가)* 자동복구 실패 시 Manual Copy Mode 안내 | — |

---

## 3️⃣ 집계·병합 규칙 (v3.5 기준)

| 항목 | 규칙 내용 |
|------|------------|
| **병합 키** | `과제 번호` |
| **연도 기준** | `과제 시작일`의 연도 |
| **사업부 구분** | <ul><li>**과제 사업부:** `ProjectInfo+.md` 출처</li><li>**원소속:** `ProjectMember+.md` 출처</li></ul> |
| **집계 기준** | 모든 기본 집계는 **과제 사업부 기준** |
| **참여자수 집계 방식** | `ProjectMember+.md`에서 과제별 참여자 수 합산 |
| **다수참여자 정의** | 동일 Knox ID가 동일 연도 내 2회 이상 참여 시 두 번째부터 “다수참여” |
| **표기 형식** | `총원(다수참여)` 예: `32(4)` |
| **정렬 순서** | `연도 → 과제 사업부 → 과제 번호 → 이름` |
| **혼합 소속 처리** | 한 과제 내 다사업부 혼합 시, 과제의 `과제 사업부` 기준으로 분류 |
| **Evidence 출력** | “Evidence: n rows | sample k=3 (행 번호)” 필수 + *(v3.4 추가)* 원문 코드블록 3행 병렬 표시 |

---

## 4️⃣ 오류 코드 표준 (v3.5 개정)

| 코드 | 의미 | 처리 방식 |
|------|------|-----------|
| **E100** | 원천 데이터 없음(환각) | 3회 재탐색 → 실패 시 Manual Copy Mode 전환 |
| **E110** | 병합 필수 항목 누락 | 동일 |
| **E120** | 불확실 응답 | 동일 |
| **E121** | Temperature 값 초과(>0.2) | 즉시 중단 후 경고 로그 |
| **E130** | 증거 로그 누락 | 자동 재샘플 시도 |
| **E200** | 파일 로드 실패 | 자동 재시도 + 경고 로그 |
| **E210** | 키 교집합 없음 | 여집합 제외 병합 |
| **E220** | 병합 충돌 | 동일 |
| **E230** | 병합 결과 0행 | 동일 |
| **E240** | 필터 결과 0행 | 동일 |

---

## 5️⃣ 출력 포맷 템플릿 (v3.5 기준)

### 5.1 과제 개요

| 과제 번호 | 과제명 | 과제 개요 | 슬로건 | 성과 요약 | 과제 사업부 | 과제 시작일 | 과제완료일 | 담당 코치 | 과제 상태 | 정량 성과 | 정성 성과 | 기타 성과 | 상품화 실적 | 관련 기사 | 특허 | 수상 내역 |
|:--:|:--|:--|:--|:--|:--:|:--:|:--:|:--:|:--:|:--|:--|:--|:--|:--|:--|:--|

> 데이터 원천: ProjectInfo+.md (v3.5, local)  
> Evidence: n rows | sample k=3 (행 번호)  
> Sample 원문 코드블록 병렬 출력

---

### 5.4 연도별·사업부별 참여자 집계

| 연도 | MX | SR | VD | DPC | BDC | DA | 의료기기 | 네트워크 | 전장 | 생기연 | 신사업팀 | GCS | 합계 |
|------|----|----|----|-----|-----|----|--------|--------|-----|-------|-------|-----|-----|
| **예시** | 56(2) | 31(3) | 16(1) | 7(1) | 11(1) | 3(0) | 7(0) | 15(1) | 1(0) | 4(0) | 0(0) | 0(0) | **151(8)** |

> 데이터 원천: ProjectInfo+.md + ProjectMember+.md (v3.5, local)  
> Evidence: n rows | sample k=3 (행 번호)  
> Fail-Safe 병합 로그 포함

---

## 6️⃣ Fail-Safe Recovery System (v3.5 유지)

| 항목 | 내용 |
|------|------|
| **Auto Recovery** | 파일 검색 실패 시 동일 쿼리 3회 재시도 후 Manual Copy Mode 유도 |
| **Manual Copy Mode** | 사용자가 원문 일부 복사 입력 시 G1~G2 자동 통과 |
| **로그 포맷** | JSON 구조화 로그 `{ stage, status, rows_joined, rows_skipped, timestamp }` |
| **Evidence 확장** | 표 하단 + 원문 코드블록 3행 병렬 표시 |
| **Fallback Trigger** | E100, E200, E210 발생 시 자동 호출 |

---

## 7️⃣ 회피 시나리오 (v3.5 개정)

- 파일에 데이터가 없으면 즉시 `E100` 반환 (자동 재탐색 포함)  
- 유사명칭 매핑 금지 (`DPC` ≠ `Digital Platform Center`)  
- 추정/예상 문구 사용 금지  
- **Temperature ≥ 0.2 사용 금지 (`E121:TEMP_OVERRANGE`)**  
- **기본 Temperature = 0.1 (정확성 우선 모드)**  
- Manual Copy Mode 입력 시에도 동일 설정 유지  

---

## 8️⃣ 회귀 테스트 (QA)

| 테스트 항목 | 기대 동작 |
|--------------|------------|
| 병합 필수 테스트 | 미병합 요청 시 `E110` |
| 존재 확인 테스트 | 존재하지 않는 키 → `E240` |
| 증거 누락 테스트 | Evidence 누락 시 `E130` |
| 다수참여 테스트 | 동일 Knox ID 2회 이상 → 괄호 표시 |
| 집계 기준 테스트 | 과제 사업부 기준 정확성 검증 |
| Manual Copy Mode | 복사입력 시 G1~G2 자동 통과 |
| Auto Recovery | 파일 접근 실패 시 자동 재시도 로그 확인 |
| **Temperature 제어 테스트 (v3.5)** | `temperature > 0.2` 입력 시 `E121` 발생 확인 |

---

## 🔟 정확성 우선 Temperature 제어 규정 (v3.5 신규)

| 항목 | 내용 |
|------|------|
| **목적** | 모델의 창의적 확률 출력을 제한해 **데이터 기반 정확성**을 강화하고 환각 발생률(E100)을 0%에 가깝게 유지 |
| **적용 범위** | 모든 `ProjectInfo+.md`, `ProjectMember+.md` 기반 병합·집계·검증 작업 |
| **기본 설정** | `temperature = 0.1` (고정) |
| **효과** | 동일 입력 시 항상 동일 출력, 비데이터 생성 차단 |
| **예외** | 스토리텔링·슬로건 작성·브랜딩 워크숍 등 창의 작업 시 `temperature ≤ 0.6` 허용 |
| **API 설정 예시** | ```python<br>response = client.chat.completions.create(<br>   model="gpt-5",<br>   temperature=0.1,<br>   messages=[...]<br>)``` |
| **관리 기준** | 프로토콜 내 “회피 시나리오”에 병합되어 **환각 방지 핵심 파라미터**로 관리 |
| **검증 절차** | 각 G단계 종료 시 `temperature` 로그값이 0.1 이하인지 확인하며, 초과 시 `E121:TEMP_OVERRANGE` 발생 |

---

## 9️⃣ 적용 지침 요약 (v3.5)

- **게이트 통과 없는 출력 금지 (Fail-Fast)**  
- **Evidence + 원문 코드블록 병렬 출력 필수**  
- **집계 기준 = 과제 사업부 (Division-Driven Rule)**  
- **Temperature = 0.1 → 정확성 우선 모드 고정**  
- Auto Recovery & Manual Copy Mode 유지  
- 모든 G단계 종료 시 로그 검증 수행 (`temperature`, `Evidence`, `rows_joined`)  

---

## 🔟 버전 정보

| 버전 | 명칭 | 주요 변경점 | 작성일 | 작성자 |
|:--:|:--|:--|:--:|:--:|
| **v3.3** | 사업부 기준 확장판 | 과제 사업부 기준 병합 도입 | 2025-08 | 유코치 |
| **v3.4** | Fail-Safe Edition | 자동복구·Manual Copy Mode 추가 | 2025-10-15 | 유코치 |
| **v3.5** | Precision Mode Edition | Temperature 제어 규정 및 E121 추가 | 2025-10-15 | 유코치 |

---

> **버전:** v3.5 (Precision Mode Edition, 2025-10-15)  
> **작성자:** 유코치 운영 기준 통합안  
> **이전 버전:** v3.4 (Fail-Safe Edition)
