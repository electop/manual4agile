# 📘 파일 기반 데이터 분석 및 집계 프로토콜 (통합 **v3.1**)

## 🔄 변경 이력
| 버전 | 일자 | 주요 변경 내용 | 작성자 |
|------|------|----------------|--------|
| v3.0 | 2024-09-01 | 최초 통합 버전 | SWITCH Secretariat |
| **v3.1** | 2025-10-15 | 자동 본문 로드 및 유효성 검증 규칙 추가 | 유코치(유주환) |

---

## 0️⃣ 목적
본 문서는 `ProjectMember+.md`와 `ProjectInfo+.md`를 기반으로  
과제 참여자·성과 정보를 분석할 때,  
**정확성(Accuracy)**, **재현성(Reproducibility)**, **자동화(Automation)** 를 보장하기 위한 표준이다.

---

## 1️⃣ 파일 기반 응답 정확도 유지 가이드
| 항목 | 원칙 |
|------|------|
| **1. 데이터 원천 제한** | 응답은 반드시 업로드된 파일 내 실제 데이터만을 기반으로 해야 한다. |
| **2. 키값 기반 탐색** | 모든 탐색은 `과제 번호`를 1차 키로 수행하고, 이후 `과제 사업부`·`이름` 등을 2차 필터로 사용한다. |
| **3. 미존재 데이터 처리** | 값이 없을 경우 `"모름"` 또는 `"데이터에 존재하지 않습니다."`로 명시한다. |
| **4. Separator 규칙** | 구성원 필드의 구분자는 쉼표(,)만 허용하며, 공백은 구분자로 사용하지 않는다. |
| **5. 다중 구성원 표기** | `<br>` 태그로 구분하며, 각 항목은 `이름(Knox ID, 사업부, 시작일, 완료일)` 형식을 따른다. |
| **6. 환각 방지** | 파일에 존재하지 않는 인물, 사업부, 과제명, 날짜, 수치는 절대 생성하지 않는다. |
| **7. 불완전 행 처리** | 파이프(`|`) 개수 불일치 등 비정상 행은 분석에서 제외하고 로그로 기록한다. |
| **8. 답변 불가능 시 대응** | 데이터 불완전 시 “파일 내 데이터가 불완전하여 답변 불가”로 명시한다. |

---

## 2️⃣ 데이터 병합 및 구조 규칙
| 항목 | 설명 |
|------|------|
| **1. 병합 키** | `과제 번호`를 기준으로 `ProjectMember+.md`와 `ProjectInfo+.md` 병합 |
| **2. 날짜 정규화** | `시작일`의 연도를 추출하여 `연도` 필드 생성 |
| **3. 중복 참여 정의** | 동일 Knox ID가 2개 이상 과제에 등장하면 **다수참여자**로 정의 |
| **4. 정렬 기준** | `연도 → '과제 사업부' → 이름` 순 |

---

## 3️⃣ 연도별·과제 사업부별 참여자수 집계 규칙
| 항목 | 설명 |
|------|------|
| **집계 단위** | 연도별 × '과제 사업부'별 |
| **연도 기준** | 과제 시작일 기준 |
| **표기 형식** | `총원(다수참여)` 예: `68(5)` |
| **합계 계산** | 연도별·'과제 사업부'별 동일 형식으로 표기 |

---

## 4️⃣ 다수참여자 집계 로직
1. 전체 기간 동안 Knox ID가 2회 이상 등장하면 다수참여자로 설정  
2. 실제 2회 이상 참여한 해부터 `다수참여자수`에 포함  
3. 첫 참여 해는 제외  
4. 세 번째 이상 참여해도 계속 포함  
5. 표기 예시: `42(3)` → 총 42명 중 3명이 두 번째 이상 참여  

---

## 5️⃣ 예외 및 경고 처리
| 상황 | 출력 규칙 |
|------|-----------|
| 사업부 없음 | `"⚠️ [과제명]의 과제 사업부는 데이터에 존재하지 않습니다."` |
| 비정규 행 | `"⚠️ 형식 오류로 제외됨"` |
| 파일 간 불일치 | `"⚠️ 파일 간 매칭되지 않음 (과제 번호: NNN)"` |

---

## 6️⃣ 출력 예시
| 연도 | MX | SR | VD | DPC | BDC | DA | 의료기기 | 네트워크 | 전장 | 생기연 | 신사업팀 | GCS | 합계 |
|------|----|----|----|-----|-----|----|--------|--------|-----|-------|-------|-----|-----|
| **2022** | 56(2) | 31(3) | 16(1) | 7(1) | 11(1) | 3(0) | 7(0) | 15(1) | 1(0) | 4(0) | 0(0) | 0(0) | **151(8)** |
| **총합** | **56(2)** | **31(3)** | **16(1)** | **7(1)** | **11(1)** | **3(0)** | **7(0)** | **15(1)** | **1(0)** | **4(0)** | **0(0)** | **0(0)** | **151(8)** |

---

## 7️⃣ 출력 형식 요약
- 기본 표기: `총원(다수참여)`  
- 사업부 누락: ⚠️ 경고 문구 표시  
- 혼합 소속: `<br>`로 구분  
- 파일 불일치: “⚠️ 데이터 불일치” 명시 후 제외  

---

## 8️⃣ 자동 파일 로드 및 탐색 순서
1. 요청 발생 시, 모든 업로드 파일 본문을 자동으로 로드하여 별도 사용자 승인 없이 접근한다.  
2. **1차 키: `과제 번호`로 전체 행 검색**  
3. **2차 필터: `과제 사업부`·`이름` 등 조건 적용**  
4. **혼합 소속 탐지 시** 자동으로 `'과제 사업부'별 세분화 병합` 수행  
5. 오류 시 `"⚠️ 혼합 소속 탐색 실패"` 로그 출력  
6. **(신규)** 업로드 즉시 본문 데이터를 메모리에 적재하며, 이후 탐색 시 별도의 샘플 요청 없이 바로 접근 가능해야 한다.  

---

## 🆕 9️⃣ 자동 본문 로드 및 유효성 검증 규칙 (v3.1 신규)
| 항목 | 설명 |
|------|------|
| **1. 자동 본문 로드** | 업로드된 모든 `.md` 파일은 시스템이 자동으로 본문을 읽어 텍스트 메모리에 적재한다. |
| **2. 파싱 시점** | 파일 업로드 직후 또는 첫 번째 분석 요청(`집계`, `검색`, `요약`) 발생 시 즉시 수행한다. |
| **3. 구조 감지 기준** | 각 파일 내 첫 번째 테이블 헤더(`| 과제번호 |`, `| 이름(` 등)를 자동 감지하여 데이터 구조를 추출한다. |
| **4. 로드 성공 기준** | 파이프(`|`) 개수가 일정하고, 헤더·본문이 1행 이상 존재하면 ‘로드 성공’으로 판정한다. |
| **5. 로드 실패 처리** | 로드 실패 시 `"⚠️ 본문 자동 로드 실패 - 샘플 표시 요청"` 메시지를 자동 출력한다. |
| **6. 수동 개입 최소화** | 로드 실패가 발생하더라도 시스템은 자동으로 5행 샘플을 추출해 사용자에게 표시한다. |
| **7. 분석 전 검증 단계** | 집계 수행 전, `파일명·행수·필수 키 존재 여부(과제번호, 사업부 등)`를 로그로 출력하여 유효성 검증을 수행한다. |

---

### ✅ 버전 v3.1 적용 효과
| 구분 | v3.0 | v3.1 |
|------|------|------|
| 본문 자동 로드 | ❌ 수동 표시 필요 | ✅ 자동 로드 및 검증 |
| 샘플 요청 | ⛔ 필요함 | ✅ 불필요 |
| 분석 재현성 | △ 조건부 | ✅ 완전 자동화 |
| 오류 대응 | 단순 경고 | 자동 로그 및 복구 절차 포함 |
